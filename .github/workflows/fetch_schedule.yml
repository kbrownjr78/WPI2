name: Daily Sports Schedule Pull

on:
  schedule:
    # Triggers automatically at 06:00 UTC (2:00 AM EST) every day
    - cron: '0 6 * * *'
  workflow_dispatch:
    # Enables the "Run workflow" manual button in the GitHub Actions UI

jobs:
  fetch-sports:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository code
      uses: actions/checkout@v4

    - name: Set up Python environment
      uses: actions/setup-python@v5
      with:
        python-version: '3.11'
        cache: 'pip' # Speeds up subsequent runs by caching dependencies

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt

    - name: Run schedule fetcher script
      # Executes the script without needing environment secrets
      run: python fetch_schedule.py

    - name: Save schedule as a workflow artifact
      uses: actions/upload-artifact@v4
      with:
        name: sports-schedule
        path: sports_schedule.json
        retention-days: 7
